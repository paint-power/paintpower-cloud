# PaintPower - Sitio simplificado

Este repositorio ha sido convertido a una versión mínima que expone únicamente una página de inicio (`/`) centrada en `https://paintpower.net`.

Contenido principal:
- `app/layout.tsx` — layout global y metadatos apuntando a `paintpower.net`.
- `app/page.tsx` — página de inicio mínima en español.

Si deseas que elimine más código o limpie componentes y archivos no usados, dime qué conservar y lo hago.